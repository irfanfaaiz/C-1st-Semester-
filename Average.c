# include <stdio.h>
int main()
{
int a,b,sum,avg;
printf("Enter any two numbers:\n");
scanf("%d%d",&a,&b);
sum = a+b;
avg = sum / 2
printf("The avg of %d and %d is \n%d.",a,b,avg);
return 0;
}
